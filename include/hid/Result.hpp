#pragma once 
#include <vector>
#include <optional>
/*
namespace HID{

    enum class Status {
        ok = 1,
        fail = 0
    };

    class Result{
        private:
            HID::Status status;
            std::vector<uint8_t> data;
        public:
            Result();
            ~Result();
            bool HasData();
            size_t Length();
            Status GetStatus();
            std::string str();
            std::optional<std::vector<uint8_t>> Data();
    };
};*/